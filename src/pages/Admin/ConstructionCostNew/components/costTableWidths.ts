/**
 * Ширина таблицы затрат (px) для ландшафтного fit-оверлея — сумма ширин колонок по виду.
 * В fitToScreen-режиме колонки идут без fixed и без Table.Summary, с колонкой «Примечание».
 * Источник чисел — ширины колонок в CostTable.tsx (держать в синхроне при правке колонок):
 *   detailed   = 650 (база) + 390 (мат/раб/суб) + 120 (Итого) + 150 (Примечание)
 *   summary    = 650 (база) + 250 (итого работы/материалы) + 120 + 150
 *   simplified = 400 (категория/вид/локализация) + 310 (Итого + Итого/ед.) + 150
 */
export const COST_TABLE_FIT_WIDTH: Record<'detailed' | 'summary' | 'simplified', number> = {
  detailed: 1310,
  summary: 1170,
  simplified: 860,
};
