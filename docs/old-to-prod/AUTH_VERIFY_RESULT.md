# Auth verify result: OLD → PROD

> This file is a **template**. It is regenerated by `npm run old-to-prod:verify-auth` after every import. Encrypted password hashes are NEVER printed in this file — only aggregate counts.

## Status: **PENDING**

`AUTH_VERIFY_OK` requires ALL of:
- counts: PROD ≥ OLD for `auth.users` and `auth.identities`
- id-set / email-set / identities.user_id-set equal between OLD export and PROD
- `sha256(encrypted_password)` match for every OLD password user; no missing in PROD; no PROD-NULL where OLD had a password
- OAuth-only OLD users never gained a password in PROD
- bootstrap identities did NOT create duplicate `(provider, provider_id)` pairs
- `FORCE_CONFIRM_EMAILS=false` → `email_confirmed_at` preserved byte-equal; `FORCE_CONFIRM_EMAILS=true` → forced count documented
- smoke login succeeded (when `MIGRATION_SMOKE_EMAIL/PASSWORD` provided)

`AUTH_VERIFY_OK_WITH_WARNINGS` is used when smoke credentials weren't provided, or smoke was skipped by policy.

`AUTH_VERIFY_FAILED` is used when any of the above hard conditions is violated.

## Counts (strict equality)

Rules:
- `auth.users`: PROD count **MUST EQUAL** OLD count. Any extra row in PROD is a hard FAIL — preexisting PROD accounts must be removed via `--clean-prod` before import, otherwise data ownership is ambiguous.
- `auth.identities`: PROD count **MUST EQUAL** `OLD count + bootstrap.created`. Bootstrap identities are documented in `import_state.bootstrapped_identities` and represent legacy OLD users who lacked an `email`-identity. Any deviation is FAIL.

| Object | OLD | Bootstrap | Expected PROD | Actual PROD | OK |
|---|---:|---:|---:|---:|---|
| auth.users      | – | — | – | – | – |
| auth.identities | – | – | – | – | – |

## Set comparison (id / email / identities.user_id)

Strict set equality between OLD export and PROD. Any missing/extra element is a hard FAIL.

| Set | OLD | PROD | Equal |
|---|---:|---:|---|
| auth.users.id | – | – | – |
| auth.users.email | – | – | – |
| auth.identities.user_id | – | – | – |

### Deltas

- ids missing in PROD: – (must be 0)
- ids extra in PROD: – (must be 0)
- emails missing in PROD: – (must be 0)
- emails extra in PROD: – (must be 0)
- identity user_ids missing in PROD: – (must be 0)

## Password byte-to-byte compare

Rule: `sha256(prod.encrypted_password) === sha256(old.encrypted_password)` for every row in the OLD export.

| Metric | Count |
|---|---:|
| Total OLD rows | – |
| Match | – |
| Mismatch | – (must be 0 for OK) |
| Missing in PROD | – (must be 0 for OK) |
| Both NULL (OAuth-only) | – |

## Password-user audit

Every OLD user with a non-NULL `encrypted_password` must have a non-NULL `encrypted_password` in PROD too. Missing rows or PROD-NULL-where-OLD-had-password → FAIL.

- total OLD password users: –
- OK in PROD: –
- missing in PROD: – (must be 0)
- PROD has NULL but OLD had a password: – (must be 0)

## OAuth-only audit

OLD users with NULL `encrypted_password` must remain password-less in PROD. Any user accidentally promoted to password-user → FAIL.

- total OLD OAuth-only users: –
- OK in PROD: –
- accidentally became password users: – (must be 0)

## Bootstrap identities

`AUTH_BOOTSTRAP_MISSING_IDENTITY_ONLY` may have created `auth.identities` rows for OLD users that lacked an `email` identity (legacy GoTrue artefact). Gated on `email` ∈ `PROD_ENABLED_AUTH_PROVIDERS`.

- bootstrap-created identities recorded in import_state: –
- duplicate (provider, provider_id) pairs in PROD: – (must be 0)

## Email-confirmation policy audit

- FORCE_CONFIRM_EMAILS at import time: –
- If FORCE_CONFIRM_EMAILS=true: users whose `email_confirmed_at` was forced to `now()`: –
- If FORCE_CONFIRM_EMAILS=false: unexpectedly changed email_confirmed_at: – (must be 0)

## Smoke login

- email_masked: `–`
- ok: –
- user_id: `–`

## Go BFF /api/v1/me

- ok: –
- role_code: `–`
- access_status: `–`

---

Final status: **PENDING**
