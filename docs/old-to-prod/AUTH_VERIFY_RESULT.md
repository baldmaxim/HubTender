# Auth verify result: OLD → PROD

> Generated by 08_verify_auth.mjs at 2026-05-20T20:21:02.989Z.
> Encrypted password hashes are NEVER printed — only aggregate counts.

## Status: **AUTH_VERIFY_OK**

## Counts (strict equality)

Rule: PROD `auth.users` count MUST EQUAL OLD count. PROD `auth.identities` MUST EQUAL `OLD + bootstrap.created` where `bootstrap.created = 29` (from `import_state.bootstrapped_identities`).

| Object | OLD | Bootstrap | Expected PROD | Actual PROD | OK |
|---|---:|---:|---:|---:|---|
| auth.users      | 33 | — | 33 | 33 | ✓ |
| auth.identities | 4 | 29 | 33 | 33 | ✓ |

## Set comparison (id / email / identities.user_id)

| Set | OLD | PROD | Equal |
|---|---:|---:|---|
| auth.users.id | 33 | 33 | ✓ |
| auth.users.email | 33 | 33 | ✓ |
| auth.identities.user_id | 4 | 33 | ✓ |

### Deltas

- ids missing in PROD: **0**
- ids extra in PROD: **0**
- emails missing in PROD: **0**
- emails extra in PROD: **0**
- identity user_ids missing in PROD: **0**

## Password byte-to-byte compare

| Metric | Count |
|---|---:|
| Total OLD rows | 33 |
| Match | 33 |
| Mismatch | 0 |
| Missing in PROD | 0 |
| Both NULL (OAuth-only) | 0 |

## Password-user audit

Every OLD user with a password must have a non-NULL encrypted_password in PROD.

- total OLD password users: **33**
- OK in PROD: **33**
- missing in PROD: **0**
- PROD has NULL but OLD had a password: **0**

## OAuth-only audit

OLD users with NULL encrypted_password must remain password-less in PROD.

- total OLD OAuth-only users: **0**
- OK in PROD: **0**
- accidentally became password users: **0**

## Bootstrap identities

- bootstrap-created identities recorded in import_state: **29**
- duplicate (provider, provider_id) pairs in PROD: **0**


## Email-confirmation policy audit

- FORCE_CONFIRM_EMAILS at import time: **false**
- unexpectedly changed email_confirmed_at: **0** (must be 0 when FORCE_CONFIRM_EMAILS=false)

## NULL token-column audit (auth.users)

Total NULLs across present token columns: **0** ✓

| Column | Present | NULL count |
|---|---|---:|
| `confirmation_token` | yes | 0 |
| `recovery_token` | yes | 0 |
| `email_change_token_new` | yes | 0 |
| `email_change_token_current` | yes | 0 |
| `email_change` | yes | 0 |
| `reauthentication_token` | yes | 0 |
| `phone_change` | yes | 0 |
| `phone_change_token` | yes | 0 |

## Generated columns in auth.identities

- `auth.identities.email` on PROD is **GENERATED ALWAYS** (Supabase Auth ≥2023.5).
- Import skipped this column; PostgreSQL computes it from `identity_data->>'email'`.
- Drift count (`email` ≠ `lower(identity_data->>'email')`): **0** ✓
- Other generated/identity columns skipped at import: _none_

## Clean-auth context (echo from import_state.clean_auth)

- executed: **YES**
- tables cleaned (deletion order): `auth.identities` → `auth.mfa_amr_claims` → `auth.mfa_challenges` → `auth.mfa_factors` → `auth.oauth_authorizations` → `auth.oauth_consents` → `auth.one_time_tokens` → `auth.refresh_tokens` → `auth.sessions` → `auth.webauthn_challenges` → `auth.webauthn_credentials` → `auth.users`
- total rows deleted: **90**

> Auth-cutover semantics:
> - auth.sessions / auth.refresh_tokens were NOT re-imported from OLD.
> - All OLD Supabase sessions are invalidated; users must log in again.
> - Password hashes were re-uploaded from OLD; users keep their OLD password.

## Smoke login

- email_masked: `o***@gmail.com`
- ok: ✓
- user_id: `d5309c31-5157-4da0-8d85-6b2cda9ba8d4`

## Go BFF /api/v1/me

Skipped (GO_BFF_BASE_URL not set or smoke login failed).

Final status: **AUTH_VERIFY_OK**
