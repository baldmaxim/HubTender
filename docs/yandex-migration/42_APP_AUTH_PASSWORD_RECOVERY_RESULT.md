# 42 — App-Auth Password Recovery Result

> Phase 6 next-phase MVP: `/forgot-password`, `/reset-password`,
> `/change-password` через Go app-auth. SMTP-mailer wired as an interface
> (NoopMailer when SMTP_HOST empty). Forgot endpoint is anti-enumeration
> by design; dev returns reset URL inline when no mailer; prod silently
> drops + logs warning if SMTP not configured.

## Final status

**APP_AUTH_PASSWORD_RECOVERY_DEPLOY_OK_WITH_SMTP_DISABLED**
— production deploy completed `2026-05-25` (release
`hubtender-api@7b3d7e3`). Backend guard verified live on
`https://tender.su10.ru`: `POST /api/v1/auth/forgot-password` returns
**503 `email_provider_not_configured`** while SMTP is unset, and the
frontend (browser smoke confirmed) surfaces the controlled
`<Result status="warning">` "Сброс пароля временно недоступен"
card — NO false-positive "письмо отправлено" toast, ZERO Supabase
Auth fallback calls. SMTP creds are still pending operator action
(see F9). Until they are added the recovery flow is deliberately
closed-shut in prod — no silent drops, no enumeration leak for
normal operations.

## Endpoints

| Method | Path | Auth | Body | Response |
|---|---|---|---|---|
| POST | `/api/v1/auth/forgot-password` | public | `{email}` | 200 `{success: true, reset_url?: "https://…?token=…"}` — reset_url ONLY in non-prod when SMTP not configured |
| POST | `/api/v1/auth/reset-password` | public | `{token, new_password}` | 204 on success |
| POST | `/api/v1/auth/change-password` | authed (Bearer) | `{current_password, new_password}` | 204 on success |

Error mapping:

| Code / endpoint | HTTP | Trigger |
|---|---|---|
| `forgot-password` (any) | 200 | always — anti-enumeration |
| `reset-password` `ErrResetTokenNotFound/Used/Expired` | 401 (same generic detail) | invalid / replayed / expired |
| `reset-password` `ErrPasswordTooShort` | 400 | < 6 chars |
| `change-password` `ErrInvalidCredentials` | 401 | wrong current password |
| `change-password` `ErrPasswordTooShort` | 400 | < 6 chars |
| `change-password` no Bearer | 401 | middleware reject |
| `change-password` `ErrAccountMissing` | 403 | user gone after JWT issued |

## Safety highlights

- **Plaintext password** hashed via `auth.HashPassword` (bcrypt cost 10, Supabase-compatible `$2a$10$…` prefix). Never logged. Never persisted other than as hash.
- **Reset token** generated by the existing 256-bit CSPRNG that mints refresh tokens. Persisted ONLY as SHA-256 hex hash in `app_auth.password_reset_tokens`. Plaintext leaves the process only in the email body (or, in dev with no mailer, in the `reset_url` field — guarded by `APP_ENV != "production"`).
- **Anti-enumeration**: `/forgot-password` always 200 with same body shape regardless of whether the email exists; mailer-not-configured in prod is silently swallowed + loud server log warning.
- **Single-use** enforced by `used_at`; **expiry** by `expires_at`; default TTL **60 min** (configurable per-Service via `WithResetTokenTTL`).
- **Refresh-token revoke** after both `Reset` and `ChangePassword` (`RevokeAllUserRefreshTokens`) — any prior session is invalidated, user must re-login on every tab/device.
- **`change-password`** re-verifies `current_password` against the live bcrypt hash before update — a compromised access token can't change credentials silently.
- **Logging**: only event types (`password_reset_requested`, `password_reset_success`, `password_changed`, `password_change_failed`, `password_reset_requested_unknown_email`); never token/hash/password values. Emails surfaced as a `sha8` 8-char digest in info logs for correlation without leaking the address.

## Email provider abstraction

[`backend/internal/auth/email.go`](../../backend/internal/auth/email.go) — `Mailer` interface + two impls:

- **`SMTPMailer`** — minimal `net/smtp` wrapper, 10s timeout, plain text body, supports STARTTLS via the SDK default when `SMTP_PORT=587`. PLAIN auth when `SMTP_USER` set.
- **`NoopMailer`** — `IsConfigured() == false`, `Send()` returns `ErrMailerNotConfigured`. Used when `SMTP_HOST` is empty.

`NewSMTPMailer(cfg)` returns `NoopMailer` when host is empty — call sites just use `mailer.IsConfigured()` for the dev/prod branch.

### Config env (new)

```env
APP_ENV=production            # development | staging | production (default: development)
APP_BASE_URL=https://tender.su10.ru    # required when SMTP_HOST set; used to build reset URLs

# All optional; empty SMTP_HOST → NoopMailer (no mail sent)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
```

On startup the BFF logs `password-recovery flow configured mailer_configured=<bool> app_env=… app_base_url=…` so operators can verify wiring at a glance.

## Changed files

### Backend (10)

| File | Δ | Purpose |
|---|---|---|
| [`backend/internal/config/config.go`](../../backend/internal/config/config.go) | +25 | `AppEnv`, `AppBaseURL`, `SMTP_*` |
| [`backend/internal/auth/email.go`](../../backend/internal/auth/email.go) | new (+110) | `Mailer` iface + `SMTPMailer` + `NoopMailer` + `ErrMailerNotConfigured` |
| [`backend/internal/auth/errors.go`](../../backend/internal/auth/errors.go) | +12 | `ErrResetTokenNotFound/Used/Expired` |
| [`backend/internal/auth/models.go`](../../backend/internal/auth/models.go) | +35 | `ForgotPasswordRequest/Result`, `ResetPasswordRequest`, `ChangePasswordRequest`, `ResetTokenRow` |
| [`backend/internal/auth/repository.go`](../../backend/internal/auth/repository.go) | +119 | `LookupAuthUserIDByEmail`, `InsertResetToken`, `FindResetTokenByHash`, `MarkResetTokenUsed`, `UpdateEncryptedPassword`, `RevokeAllUserRefreshTokens` |
| [`backend/internal/auth/service.go`](../../backend/internal/auth/service.go) | +200 | `Forgot/Reset/ChangePassword` + `WithMailer/WithAppEnv/WithAppBaseURL/WithResetTokenTTL` builder pattern + `buildResetURL` + `sha8` helper |
| [`backend/internal/auth/handlers.go`](../../backend/internal/auth/handlers.go) | +110 | `ForgotPassword/ResetPassword/ChangePassword` |
| [`backend/cmd/server/main.go`](../../backend/cmd/server/main.go) | +18 | mailer wiring + 3 new routes (forgot/reset public, change-password authed) |
| [`backend/internal/auth/service_test.go`](../../backend/internal/auth/service_test.go) | +210 | 11 new tests + 6 new fakeRepo methods |

### Frontend (3)

| File | Δ | Purpose |
|---|---|---|
| [`src/lib/auth/client.ts`](../../src/lib/auth/client.ts) | +85 | `forgotPassword`, `resetPassword`, `changePassword`, `ForgotPasswordResult` type |
| [`src/pages/Auth/ForgotPassword.tsx`](../../src/pages/Auth/ForgotPassword.tsx) | −25 / +30 | App-mode branch: calls `appAuthForgot`; dev-only `reset_url` displayed in a `<Card type="warning">` for operator testing |
| [`src/pages/Auth/ResetPassword.tsx`](../../src/pages/Auth/ResetPassword.tsx) | −22 / +95 | Replaced `ResetPasswordAppPlaceholder` with full `ResetPasswordApp`: reads `?token=` from URL, posts to `/reset-password`, success → redirect to `/login` after 800 ms |

`/change-password` UI **not** added in this PR — backend endpoint is ready; the client helper is exported but tree-shaken from the current bundle (no caller yet). See follow-ups.

## Test / build / typecheck / lint

| Check | Result |
|---|---|
| `gofmt -l` (touched files) | ✅ clean |
| `go test ./internal/auth` | ✅ 11 new tests pass (forgot generic, reset OK + revoke + single-use + expiry + unknown + weak password; change-password OK + wrong current + weak new) |
| `go test ./internal/handlers ./internal/repository ./internal/services` | ✅ |
| `go build ./cmd/server` | ✅ |
| `npm run typecheck` | ✅ |
| `npm run lint -- --max-warnings 0` | ✅ |
| `npm run build:prod` | ✅ (~1m 7s, Sentry sourcemaps uploaded) |

### Bundle inspection

| Probe | Count |
|---|---|
| `/api/v1/auth/forgot-password` | 1 ✅ |
| `/api/v1/auth/reset-password` | 1 ✅ |
| `/api/v1/auth/change-password` | 0 (no UI caller yet — tree-shaken; backend route is live regardless) |

### Test coverage highlights

- `TestForgot_UnknownEmailGenericSuccess` — anti-enumeration confirmed (no `reset_url` for unknown email)
- `TestForgot_KnownEmail_NonProdReturnsResetURL` — dev convenience; verifies the URL contains a plaintext token (NOT the stored hash)
- `TestForgot_KnownEmail_ProdHidesResetURL` — `APP_ENV=production` MUST never expose `reset_url`
- `TestReset_OK_RevokesRefreshTokensAndAcceptsNewPassword` — full round-trip: forgot → reset → old password rejected → new password authenticates → pre-existing refresh token revoked
- `TestReset_TokenSingleUse` — replaying a used reset token returns `ErrResetTokenUsed`
- `TestReset_ExpiredTokenFails` — 1ns TTL expiry rejected with `ErrResetTokenExpired`
- `TestReset_UnknownTokenFails` — unknown plaintext returns `ErrResetTokenNotFound`
- `TestReset_WeakPasswordFails` — `ErrPasswordTooShort`
- `TestChangePassword_OK_RevokesRefreshTokens` — happy path + refresh-revoke verification
- `TestChangePassword_WrongCurrentFails` — `ErrInvalidCredentials`
- `TestChangePassword_WeakNewFails` — `ErrPasswordTooShort`

## Production deploy gate (post-commit hardening)

After the initial F42 ship, the operator flagged that the silent-drop
behaviour in prod was too risky — users would see "we sent you a letter"
toast but no email would arrive. To fix this without removing the dev
convenience path:

- **`Service.Forgot`** now checks `appEnv == "production" &&
  !mailer.IsConfigured()` at the **top** of the function, BEFORE any
  token is minted, and returns `ErrMailerNotConfigured`. The handler
  maps this to **HTTP 503** with `detail: "email_provider_not_configured"`.
- **`Handler.ForgotPassword`** retains the generic 200 success for all
  other branches (including unknown email AND email-send failure when
  mailer IS configured), preserving anti-enumeration for the
  normal-operations case.
- **Frontend `ForgotPassword.tsx`** maps the 503 to a dedicated
  `<Result status="warning">` card titled "Сброс пароля временно недоступен"
  with the operator-handoff instruction. NO false-positive toast fires.

Trade-off: in this ONE branch we leak that "email service is down" to
any caller (vs. anti-enumeration). Justified — false-positive UX would
be worse and would harm real users with no recourse. Anti-enumeration
for normal operations (unknown email, transient SMTP error with
configured provider) is unaffected.

### Tests covering the gate

- `TestForgot_ProdWithoutSMTP_Returns503Error` — prod + NoopMailer →
  `ErrMailerNotConfigured`; verifies NO reset token persisted.
- `TestForgot_ProdWithSMTP_OKHidesResetURL` — prod + configured fake
  mailer → 200, no reset_url leak, email actually sent.
- `TestForgot_ProdWithSMTP_UnknownEmailGenericSuccess` — prod + mailer
  configured + unknown email → generic 200, no email sent (anti-enum
  preserved within "normal operations").
- `TestForgot_KnownEmail_NonProdReturnsResetURL` — non-prod + NoopMailer
  → 200 with reset_url inline (unchanged from initial F42).
- `TestForgot_UnknownEmailGenericSuccess` — non-prod + unknown email →
  generic 200 (unchanged).

## Production deploy result (2026-05-25)

Operator performed `bash scripts/deploy-production.sh both` after the
`7b3d7e3` push. Initial post-deploy probes still showed silent-drop
200 — investigation traced this to **`APP_ENV` and `APP_BASE_URL`
missing from `/srv/sites/tender.su10.ru/server/.env.prod`** (the
config defaulted `APP_ENV=development`, so the guard predicate
`appEnv == "production" && !mailer.IsConfigured()` was always false).
Operator added both lines and restarted `hubtender-bff.service`.

Post-fix startup log (BFF):

```
release=hubtender-api@7b3d7e3
mailer_configured=false
app_env=production
app_base_url=https://tender.su10.ru
kid=xDItcfIj5Kjn_UU9uYOcVl93afQYpNDiLlQCE1EAeag
iss=https://tender.su10.ru
```

Public probes from operator workstation:

| Probe | Expected | Actual |
|---|---|---|
| `POST /api/v1/auth/forgot-password` `{email:"test@example.com"}` | 503 `email_provider_not_configured` | ✅ 503 `email_provider_not_configured` |
| `GET /.well-known/jwks.json` | 200, kid matches | ✅ kid `xDItcfIj5Kjn_UU9uYOcVl93afQYpNDiLlQCE1EAeag` |
| `POST /api/v1/auth/register` `{}` | 400 validation | ✅ 400 `invalid email` |
| `POST /api/v1/auth/reset-password` `{}` | 401/400 validation | ✅ 401 `invalid or expired reset token` |
| `GET /health`, `/health/db` | (BFF) 200 | n/a — nginx routes non-`/api/*` to SPA index; BFF health is internal only by design |

### Browser smoke (2026-05-25)

Operator-driven manual verification on `https://tender.su10.ru` after
the `.env.prod` fix + restart:

| Step | Observation |
|---|---|
| Open `/forgot-password` | Page rendered (app-mode branch) |
| Submit valid email via form | `POST /api/v1/auth/forgot-password` dispatched |
| Network panel | **HTTP 503** with `detail: email_provider_not_configured` |
| UI state | `<Result status="warning">` card — "Сброс пароля временно недоступен. Обратитесь к администратору." |
| False-positive toast "письмо отправлено" | ❌ NOT shown — anti-foot-gun verified |
| Supabase Auth network calls | **0** — app-mode branch fully active, no legacy fallback |
| Regression: `/login`, `/register`, `/logout` in same session | ✅ all three work normally — guard change isolated to forgot-password path |

End-to-end contract for the closed-shut state is confirmed: backend
guard, frontend 503-handler, and the `providerUnavailable` UI all
behave as designed. Login / register / logout flows are unaffected
by the guard. The system is safe to leave in production indefinitely
until SMTP creds land (F9).

### Final production deploy smoke (2026-05-25, post `a0fef3c`)

Operator deployed origin/main (HEAD `a0fef3c`, includes F42 +
insurance redistribution unification) via
`bash scripts/deploy-production.sh both`. Frontend backup snapshot
created at `/srv/sites/tender.su10.ru/backups/public/public.backup-20260525-105041`.

Harness-side public probes from operator workstation:

| Probe | Expected | Actual |
|---|---|---|
| `POST /api/v1/auth/forgot-password` `{email:"smoke-test@example.com"}` | 503 `email_provider_not_configured` | ✅ 503 `email_provider_not_configured` — guard persists post-deploy |
| `POST /api/v1/auth/register` `{}` | 400 validation | ✅ 400 `invalid email` |
| `POST /api/v1/auth/reset-password` `{}` | 401 validation | ✅ 401 `invalid or expired reset token` |
| `POST /api/v1/auth/login` `{}` | 401 invalid credentials | ✅ 401 `invalid credentials` |
| `GET /api/v1/auth/me` (no Bearer) | 401 | ✅ 401 `missing or malformed Authorization header` |
| `GET /.well-known/jwks.json` | 200, kid `xDItcfIj5Kjn_UU9uYOcVl93afQYpNDiLlQCE1EAeag` | ✅ matches |

Frontend bundle inspection (`https://tender.su10.ru/`):

| Probe | Result |
|---|---|
| Main bundle hash | `index-DJcziXKM.js` (refreshed from prior `index-DXZc_mr4.js`) |
| Inlined `/api/v1/auth/forgot-password` | ✅ 1 occurrence |
| Inlined `/api/v1/auth/login` | ✅ 1 occurrence |
| Inlined `/api/v1/auth/register` | ✅ 1 occurrence |
| Inlined `/api/v1/auth/me` | ✅ 1 occurrence |
| Legacy `supabase.co/auth/v1/token` | **0 occurrences** — Supabase Auth runtime path fully eliminated from production bundle |

Browser-side behaviour delegated to operator (already confirmed
green earlier in this doc: `/forgot-password` UI shows the
controlled warning card, no false-positive toast, 0 Supabase Auth
network calls, `/login`+`/register`+`/logout` work normally).

**Status:** `APP_AUTH_PASSWORD_RECOVERY_DEPLOY_OK_WITH_SMTP_DISABLED`
re-confirmed against the live production deployment.

### Security note: APP_JWT key rotation

During the deploy session, the active `APP_JWT_PRIVATE_KEY_B64` value
was inadvertently shared in chat (middle portion was redacted by the
operator, but the BEGIN/END markers and significant base64 length were
still in clear). Per recommendation the key was rotated immediately:

- **Old kid** `gpJuRL85-hNX4LSxEq05UAz1lOt2PwFlS4gYRP7BzMU` — retired.
- **New kid** `xDItcfIj5Kjn_UU9uYOcVl93afQYpNDiLlQCE1EAeag` — active.

Effect on users: every access token issued before
`2026-05-25T09:37:33Z` will fail signature verification on the next
API call; clients will use their refresh token (still valid against
`app_auth.refresh_tokens` row, unrelated to JWT signing key) to mint
a new access token signed by the new key. No data loss; users may see
one transient re-auth round-trip. JWKS endpoint serves the new key
only — clients that cache the previous JWKS for >5 min should pick up
the new key on their next fetch.

## Deploy recommendation

Backend + frontend both:

```bash
# After git push origin main:
bash scripts/deploy-production.sh both
```

**Before** flipping the `/forgot-password` UI to the new app-mode flow for end users, the operator MUST:

1. Add real SMTP creds to `/srv/sites/tender.su10.ru/server/.env.prod`:
   ```
   APP_ENV=production
   APP_BASE_URL=https://tender.su10.ru
   SMTP_HOST=<provider>
   SMTP_PORT=587
   SMTP_USER=<user>
   SMTP_PASSWORD=<password>
   SMTP_FROM="TenderHUB <noreply@su10.ru>"
   ```
2. Restart BFF (`bash scripts/deploy-production.sh backend`) and verify
   the startup log line: `mailer_configured=true app_env=production`.
3. Send a test reset to an operator-owned email and confirm delivery.

Until step 3 is verified, users on prod hitting `/forgot-password` will
see the generic success toast but NO email — by design (anti-enumeration)
no error surfaces to them. The operator's server log will show
`forgot-password called but mailer is not configured in production`.

### Post-deploy operator smoke (browser)

1. Open `/forgot-password`, submit an existing email.
2. Expect generic "Если email зарегистрирован..." toast.
3. Operator (or recipient) clicks the link in email → `/reset-password?token=…`.
4. Enter new password ≥ 6 chars → 204 → redirect to `/login` after 800 ms.
5. Login with new password → 200. Old password → 401.
6. All other tabs / devices: next API call → 401 (refresh tokens were revoked).
7. Sanity: submit an UNKNOWN email at `/forgot-password` → same generic toast, no email sent.
8. Sanity: replay the used reset link → 401 toast.

## Rollback note

- Frontend rollback: redeploy a prior bundle (or restore `public.backup-…`). Old Supabase-mode forgot/reset returns. Backend new endpoints stay (idle until called).
- Backend rollback: revert the commit, redeploy backend. Frontend will get 404 on `/forgot-password` POST → toast "forgot-password failed" — degraded but not data-corrupting.
- DB rollback: no schema changes; only INSERTs into `app_auth.password_reset_tokens`. Stale rows expire automatically via `expires_at`.

## Open notes / follow-ups

| # | Item | Severity |
|---|---|---|
| F9 | **SMTP credentials in prod** — without them, `/forgot-password` is functionally broken for end users. Operator task. | **P1 (deploy gate)** |
| F10 | Change-password UI surface — currently no profile page exposes the endpoint. Backend + client helper are ready. | P2 |
| F11 | Rate limiting on `/forgot-password` (per-IP + per-email). Currently the BFF will gladly accept dozens of forgot requests; an attacker can spam mailbox or burn the DB row. Reasonable lower bound: 1 request per email per 60 s. | P2 |
| F12 | Reset / change-password events visible in `app_auth.auth_events` but NOT in the admin notifications fan-out. If desired, add a notification row "user X reset password" so admins see security-relevant activity. | P3 |
| F13 | HTML mail template — currently plain text. Sufficient for MVP but a branded HTML body (with logo / styled button) is friendlier. | P3 |
| F14 | `Reply-To` and `Return-Path` headers — add when SMTP setup actually has a deliverable inbox. | P3 |
| F15 | `RevokeAllUserRefreshTokens` is best-effort: a logged-out user with cached access token can keep using the API until their 15-min access TTL elapses. Acceptable trade-off; consider a server-side allowlist for `change-password`-induced sessions if stricter is required. | P3 |

## What was NOT done (per spec)

- ✅ deploy completed by operator on `2026-05-25` (initial F42 + post-commit guard hardening)
- ✅ push (`7b3d7e3` + prior commits) on `origin/main`
- ❌ DATABASE_URL / AUTH_MODE untouched
- ❌ import / clean / repair not run
- ❌ Supabase SDK / fallback NOT removed
- ❌ change-password UI surface deferred (see F10)
- ❌ SMTP creds NOT added (operator decision, see F9) — guard now ensures this is closed-shut, not silent-drop

## Related docs

- [29_APP_AUTH_SCHEMA_PLAN.md](29_APP_AUTH_SCHEMA_PLAN.md) — `app_auth.password_reset_tokens` schema
- [32_APP_AUTH_BACKEND_MVP_RESULT.md](32_APP_AUTH_BACKEND_MVP_RESULT.md) — login/refresh/logout/me/jwks
- [38_APP_AUTH_CUTOVER_RESULT.md](38_APP_AUTH_CUTOVER_RESULT.md) — prod cutover
- [41_APP_AUTH_REGISTER_RESULT.md](41_APP_AUTH_REGISTER_RESULT.md) — register endpoint
- **42 (this)** — password recovery (forgot/reset/change)
