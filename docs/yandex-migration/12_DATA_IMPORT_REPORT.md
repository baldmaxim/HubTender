# 12. DATA IMPORT REPORT

> Generated by `scripts/prod-to-yandex/04_import_yandex.mjs`.
> Overwritten on every run. Source = PROD Supabase export only.
> This is a placeholder until the first import run.

Sections (populated on run):

- Run metadata + options (two-key guard flags)
- Emptiness precheck (all target tables 0 rows)
- Clean-yandex (explicit-list DELETE, reverse FK order, no CASCADE)
- Auth phase (auth.users / auth.identities, encrypted_password byte-for-byte)
- Public tables (per-table inserted / skipped / triggers disabled)
- Triggers disabled during import (re-enabled in finally; kept in final schema)

## Final status

```
NOT_RUN
```
